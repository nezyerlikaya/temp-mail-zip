# Modules

This directory is the home for bounded application modules in the Temp Mail SaaS modular monolith.

Rules:

- One owner per concept.
- Do not create placeholder or future modules.
- Do not add business logic in STEP001.
- Create modules only when a later scoped prompt assigns that ownership.
- Keep shared Laravel infrastructure in the standard framework directories.
