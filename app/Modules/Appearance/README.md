# Appearance Module

Appearance owns safe presentation configuration boundaries.

Scope:

- Approved token values.
- Safe theme selection boundaries.
- Approved homepage section and ad-slot configuration.

Rules:

- No arbitrary HTML editors.
- No arbitrary CSS editors.
- No arbitrary JavaScript editors.
- No raw style injection.
- No page builder.

Sections are controlled registry entries. Empty or unavailable sections must not render.
